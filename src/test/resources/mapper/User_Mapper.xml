<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.zzb.mybatis.test.dao.ISkuDao">

    <!-- resultMap：将数据库下划线列名映射到 SkuVO 驼峰属性 -->
    <resultMap id="skuVOMap" type="cn.zzb.mybatis.test.po.SkuVO">
        <id column="id" property="id"/>
        <result column="source" property="source"/>
        <result column="goods_id" property="goodsId"/>
        <result column="goods_name" property="goodsName"/>
        <result column="original_price" property="originalPrice"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="querySkuVOById" parameterType="java.lang.Long" resultMap="skuVOMap">
        SELECT id, source, goods_id, goods_name, original_price, create_time, update_time
        FROM sku
        WHERE id = #{id}
    </select>

    <select id="querySkuById" parameterType="java.lang.Long" resultType="cn.zzb.mybatis.test.po.Sku">
        SELECT id, source, goods_id, goods_name, original_price, create_time, update_time
        FROM sku
        WHERE id = #{id}
    </select>

    <select id="querySkuByInfo" parameterType="cn.zzb.mybatis.test.po.Sku" resultType="cn.zzb.mybatis.test.po.Sku">
        SELECT id, source, goods_id, goods_name, original_price, create_time, update_time
        FROM sku
        WHERE id = #{id} AND source = #{source}
    </select>

    <insert id="insertSku" parameterType="cn.zzb.mybatis.test.po.Sku">
        INSERT INTO sku (source, channel, goods_id, goods_name, original_price)
        VALUES (#{source}, #{channel}, #{goods_id}, #{goods_name}, #{original_price})

        <selectKey keyProperty="id" order="AFTER" resultType="long">
            SELECT LAST_INSERT_ID()
        </selectKey>

    </insert>

    <update id="updateSku" parameterType="cn.zzb.mybatis.test.po.Sku">
        UPDATE sku
        SET goods_name = #{goods_name}, original_price = #{original_price}
        WHERE id = #{id}
    </update>

    <delete id="deleteSkuById" parameterType="java.lang.Long">
        DELETE FROM sku WHERE id = #{id}
    </delete>

    <!-- 动态 SQL：if + where 标签，按条件查询 -->
    <select id="querySkuByCondition" parameterType="cn.zzb.mybatis.test.po.Sku" resultType="cn.zzb.mybatis.test.po.Sku">
        SELECT id, source, channel, goods_id, goods_name, original_price, create_time, update_time
        FROM sku
        <trim prefix="WHERE" prefixOverrides="AND">
            <if test="source != null and source != ''">
                AND source = #{source}
            </if>
            <if test="goods_name != null and goods_name != ''">
                AND goods_name = #{goods_name}
            </if>
            <if test="channel != null and channel != ''">
                AND channel = #{channel}
            </if>
        </trim>
    </select>


</mapper>
